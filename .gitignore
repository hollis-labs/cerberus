/cerberus
/bin/
/dist/
*.log

# Web UI: source deps and generated bundle are not tracked.
web/node_modules/
/internal/webui/dist/*
!/internal/webui/dist/.gitkeep

# Claude Code client runtime state (worktrees, settings.local.json, locks).
# Agent configuration lives in AGENTS.md; nothing here should be tracked.
/.claude/

# Source-tree build coordination (lock inode must persist).
.cerberus-build.lock
.DS_Store
